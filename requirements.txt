# === 핵심 (앱 실행 필수) ===
# 사용자 정책 2026-05-04 — exact pinning. 집/회사 PC 빌드 환경 일관성 + upstream
# API 변경 회귀 회피. 새 환경 만들 때 동일 버전 보장.
numpy==2.4.4
scipy==1.17.1
PySide6==6.11.0
pyqtgraph==0.14.0
PyOpenGL==3.1.10

# === 확장 대비 ===
pandas==3.0.2     # 클립보드 TSV 파싱 편의 (pd.read_clipboard 등)
chardet==7.4.3    # 파일 입력 확장 시 인코딩 감지 (Profile Vision 참고)
