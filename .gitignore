venv/
__pycache__/
*.pyc
*.pyo
.vscode/
.idea/

# 사용자별 런타임 파일 (앱이 자동 생성·갱신)
settings.json
coord_library.json
*.bak

# 디버그/샘플/검증용 산출물 — 모두 debug/ 한 곳에 모음
debug/

# 작업 노트 / 임시 문서 (외부 LLM 리뷰 2026-05-01)
docs/homework_combined.txt

# stylesheet 가 동적으로 만드는 체크박스 SVG (테마 accent 색별)
assets/check_*.svg
# stylesheet 가 동적으로 만드는 스크롤바 화살표 SVG (테마 text 색별)
assets/arrow_*.svg

# 로그·캐시 (아직 폴더 없음, 추후 대비)
logs/
cache/

# family_coord 정책 회귀 검증 — current 결과는 매 실행마다 갱신 (transient)
tests/current/

# PyInstaller 빌드 산출물 (배포판은 GitHub Release로만 공유)
build/
dist/
*.spec
# 표준 빌드 스펙만 track (실험용 임시 spec 들은 계속 무시)
!Wafer Map.spec
Wafer_Map_*.zip
